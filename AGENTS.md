# Instructions for agents for this project

After each change, make sure that tests and linters parse and that the code is formatted. Use `cargo check`
and `cargo clippy` for linting and `cargo fmt` for formatting. Use `cargo test` to run tests.
